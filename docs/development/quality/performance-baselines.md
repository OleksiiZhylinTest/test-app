# Performance Baselines

## Purpose

This document records established latency baselines and regression thresholds for the test-app report pipeline. Each row represents a measured scenario with its acceptable threshold. Threshold changes require human approval (see GH Performance QA SDLC Gates). Baselines are measured and updated by `gh-performance-qa`.

| Scenario | Baseline (ms) | Threshold (ms) | Measured | Last Updated |
|----------|--------------|----------------|----------|--------------|
